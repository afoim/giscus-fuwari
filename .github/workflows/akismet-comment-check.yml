name: Akismet Discussion Comment Checker
on:
  discussion_comment:
    types: [created]

jobs:
  check-akismet:
    runs-on: ubuntu-latest
    steps:
      - name: Check and delete spam comments
        env:
          AKISMET_API_KEY: ${{ secrets.AKISMET_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
          COMMENT_BODY: ${{ github.event.comment.body }}
          COMMENT_AUTHOR: ${{ github.event.comment.user.login }}
          COMMENT_URL: ${{ github.event.comment.url }}
          COMMENT_HTML_URL: ${{ github.event.comment.html_url }}
          DISCUSSION_TITLE: ${{ github.event.discussion.title }}
          REPO_OWNER: ${{ github.repository_owner }}
          REPO_NAME: ${{ github.event.repository.name }}
        run: |
          set -uo pipefail
          
          echo "============================================"
          echo "🤖 AKISMET SPAM DETECTION STARTED"
          echo "============================================"
          echo "⏰ Timestamp: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "👤 Comment Author: ${COMMENT_AUTHOR}"
          echo "📝 Comment Length: ${#COMMENT_BODY} characters"
          echo "🗂️ Repository: ${REPO_OWNER}/${REPO_NAME}"
          echo "💬 Discussion: ${DISCUSSION_TITLE}"
          echo "🔗 Comment URL: ${COMMENT_HTML_URL}"
          echo ""
          
          echo "📄 COMMENT CONTENT:"
          echo "----------------------------------------"
          echo "${COMMENT_BODY}"
          echo "----------------------------------------"
          echo ""
          
          # 验证必要的环境变量
          echo "🔧 ENVIRONMENT CHECK:"
          if [[ -z "${AKISMET_API_KEY:-}" ]]; then
            echo "❌ AKISMET_API_KEY is not set"
            exit 1
          else
            echo "✅ AKISMET_API_KEY: ${AKISMET_API_KEY:0:8}..."
          fi
          
          if [[ -z "${GITHUB_TOKEN:-}" ]]; then
            echo "❌ GITHUB_TOKEN is not set"
            exit 1
          else
            echo "✅ GITHUB_TOKEN: ${GITHUB_TOKEN:0:8}..."
          fi
          echo ""
          
          # 构建 Akismet API 请求
          echo "🌐 AKISMET API REQUEST:"
          AKISMET_URL="https://${AKISMET_API_KEY}.rest.akismet.com/1.1/comment-check"
          echo "📡 API Endpoint: ${AKISMET_URL:0:30}...comment-check"
          echo "🏠 Blog URL: https://github.com/${REPO_OWNER}/${REPO_NAME}"
          echo "🖥️ User IP: 127.0.0.1 (GitHub Actions default)"
          echo "🕷️ User Agent: GitHubActionBot/1.0"
          echo "📝 Comment Type: comment"
          echo ""
          
          echo "🔄 Sending request to Akismet..."
          AKISMET_RESPONSE=$(curl -s -w "\nHTTP_CODE:%{http_code}\nTOTAL_TIME:%{time_total}" \
            -X POST "$AKISMET_URL" \
            -H "Content-Type: application/x-www-form-urlencoded" \
            -H "User-Agent: GitHubActionBot/1.0" \
            --data-urlencode "blog=https://github.com/${REPO_OWNER}/${REPO_NAME}" \
            --data-urlencode "user_ip=127.0.0.1" \
            --data-urlencode "user_agent=GitHubActionBot/1.0" \
            --data-urlencode "comment_type=comment" \
            --data-urlencode "comment_author=${COMMENT_AUTHOR}" \
            --data-urlencode "comment_content=${COMMENT_BODY}" \
            2>/dev/null || echo "ERROR")
          
          # 解析响应
          SPAM_CHECK_RESULT=$(echo "$AKISMET_RESPONSE" | head -n1)
          HTTP_CODE=$(echo "$AKISMET_RESPONSE" | grep "HTTP_CODE:" | cut -d: -f2)
          RESPONSE_TIME=$(echo "$AKISMET_RESPONSE" | grep "TOTAL_TIME:" | cut -d: -f2)
          
          echo "📊 AKISMET RESPONSE:"
          echo "📈 HTTP Status: ${HTTP_CODE:-unknown}"
          echo "⏱️ Response Time: ${RESPONSE_TIME:-unknown}s"
          echo "🎯 Spam Detection Result: '${SPAM_CHECK_RESULT}'"
          echo ""
          
          # 检查是否为垃圾评论
          if [ "$SPAM_CHECK_RESULT" = "true" ]; then
            echo "🚨 SPAM DETECTED!"
            echo "============================================"
            echo "🗑️ Attempting to delete spam comment..."
            echo ""
            
            echo "🔍 GitHub API DELETE Request:"
            echo "📡 Endpoint: ${COMMENT_URL}"
            echo "🔑 Authorization: token ${GITHUB_TOKEN:0:8}..."
            echo "🕷️ User-Agent: AkismetBot/1.0"
            echo ""
            
            DELETE_RESPONSE=$(curl -s -w "\nHTTP_CODE:%{http_code}\nTOTAL_TIME:%{time_total}" \
              -X DELETE "$COMMENT_URL" \
              -H "Authorization: token ${GITHUB_TOKEN}" \
              -H "Accept: application/vnd.github+json" \
              -H "User-Agent: AkismetBot/1.0" \
              2>/dev/null || echo "ERROR")
            
            DELETE_HTTP_CODE=$(echo "$DELETE_RESPONSE" | grep "HTTP_CODE:" | cut -d: -f2)
            DELETE_TIME=$(echo "$DELETE_RESPONSE" | grep "TOTAL_TIME:" | cut -d: -f2)
            DELETE_BODY=$(echo "$DELETE_RESPONSE" | head -n1)
            
            echo "📊 DELETE RESPONSE:"
            echo "📈 HTTP Status: ${DELETE_HTTP_CODE:-unknown}"
            echo "⏱️ Response Time: ${DELETE_TIME:-unknown}s"
            
            if [ "$DELETE_HTTP_CODE" = "204" ]; then
              echo "✅ SUCCESS: Spam comment deleted successfully"
              echo "🎉 Comment from @${COMMENT_AUTHOR} has been removed"
            else
              echo "❌ FAILED: Could not delete comment"
              echo "🔍 HTTP Status Code: ${DELETE_HTTP_CODE}"
              if [[ -n "$DELETE_BODY" && "$DELETE_BODY" != "ERROR" ]]; then
                echo "📄 Response Body: ${DELETE_BODY}"
              fi
              
              # 尝试获取更多错误信息
              echo ""
              echo "🔍 DEBUGGING - Checking comment accessibility..."
              CHECK_RESPONSE=$(curl -s -w "HTTP_CODE:%{http_code}" \
                -H "Authorization: token ${GITHUB_TOKEN}" \
                -H "Accept: application/vnd.github+json" \
                -H "User-Agent: AkismetBot/1.0" \
                "$COMMENT_URL" 2>/dev/null || echo "ERROR")
              
              CHECK_HTTP_CODE=$(echo "$CHECK_RESPONSE" | grep "HTTP_CODE:" | cut -d: -f2)
              echo "🔍 Comment Check Status: ${CHECK_HTTP_CODE:-unknown}"
              
              if [ "$CHECK_HTTP_CODE" = "200" ]; then
                echo "ℹ️ Comment exists and is accessible"
              elif [ "$CHECK_HTTP_CODE" = "404" ]; then
                echo "ℹ️ Comment not found (may have been deleted already)"
              elif [ "$CHECK_HTTP_CODE" = "403" ]; then
                echo "⚠️ Access forbidden - check token permissions"
              else
                echo "⚠️ Unexpected status when checking comment"
              fi
            fi
            
          elif [ "$SPAM_CHECK_RESULT" = "false" ]; then
            echo "✅ CLEAN COMMENT"
            echo "============================================"
            echo "🟢 Akismet determined this is NOT spam"
            echo "✨ Comment from @${COMMENT_AUTHOR} is allowed"
            
          else
            echo "⚠️ AKISMET ERROR"
            echo "============================================"
            echo "❌ Unexpected response from Akismet API"
            echo "📄 Raw Response: '${SPAM_CHECK_RESULT}'"
            echo "📈 HTTP Status: ${HTTP_CODE:-unknown}"
            
            if [ "$HTTP_CODE" != "200" ]; then
              echo "🔍 This may indicate an API key issue or service problem"
            fi
          fi
          
          echo ""
          echo "============================================"
          echo "🏁 AKISMET SPAM DETECTION COMPLETED"
          echo "⏰ End Time: $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          echo "============================================"
